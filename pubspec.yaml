name: starter_app
description: A new Flutter application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  stacked: ^1.7.1+1
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true